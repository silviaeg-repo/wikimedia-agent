"""The evaluation harness (§5).

Layer 3: the only part of this project that spends money, and it never runs by
accident -- excluded from the default ``pytest`` run, absent from CI, invoked
explicitly with a scope.
"""
